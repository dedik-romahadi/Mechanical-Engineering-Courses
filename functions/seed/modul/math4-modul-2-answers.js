/* eslint-disable max-len */
/**
 * math4-modul-2 — answer-key metadata.
 *
 * Auto-generated by functions/seed/extract-modul-answers.js dari source HTML.
 * Edit source HTML, lalu re-extract; JANGAN edit file ini manual.
 *
 * Format: MC entries pakai letter answer (A/B/C/D), Comp pakai numeric answer +
 * tolerance. Explain WAJIB ada (modul reveal jawaban setelah submit untuk
 * pembelajaran formative).
 */

const MC = [
  { qId: "mc1", type: "mc", points: 1, answer: "B", explain: "<strong>Variabel terpisah</strong> jika dy/dx dapat ditulis sebagai produk fungsi-x dan fungsi-y. Opsi (B) <code>dy/dx = xy = x · y</code> ✓. Opsi (A) dan (C) ada suku campuran <code>x+y</code> tidak bisa difaktorkan. Opsi (D) PD orde 2 (bukan orde 1)." },
  { qId: "mc2", type: "mc", points: 1, answer: "B", explain: "Trick: <strong>faktorisasi</strong>. <code>xy + x = x(y+1)</code>. Setelah itu PD jadi <code>dy/dx = x·(y+1)</code> — sekarang produk fungsi-x dan fungsi-y. Pisahkan: <code>dy/(y+1) = x dx</code>. Inilah teknik Contoh 2.2." },
  { qId: "mc3", type: "mc", points: 1, answer: "B", explain: "Pisahkan: <code>dy/y = r dx</code>. Integrasi: <code>ln|y| = rx + C</code>. Eksponensiasi: <code>y = e<sup>rx+C</sup> = e<sup>C</sup>·e<sup>rx</sup> = C₁·e<sup>rx</sup></code>. Solusi umum eksponensial — pola universal untuk growth/decay." },
  { qId: "mc4", type: "mc", points: 1, answer: "B", explain: "PD <code>dT/dt = -k(T-T<sub>m</sub>)</code>: pisahkan <code>T-T<sub>m</sub></code> ke kiri (bagi kedua ruas dengan <code>(T-T<sub>m</sub>)</code>) dan <code>dt</code> ke kanan: <code>dT/(T-T<sub>m</sub>) = -k dt</code>. Sekarang siap diintegrasikan." },
  { qId: "mc5", type: "mc", points: 1, answer: "A", explain: "Integrasi <code>dT/(T-T<sub>m</sub>) = -k dt</code>: <code>ln|T-T<sub>m</sub>| = -kt + C</code> → <code>T - T<sub>m</sub> = C₁e<sup>-kt</sup></code>. Apply IC <code>T(0)=T₀</code>: <code>T₀ - T<sub>m</sub> = C₁</code>. Solusi: <code>T(t) = T<sub>m</sub> + (T₀-T<sub>m</sub>)e<sup>-kt</sup></code>. Asimptot horizontal di T<sub>m</sub>." },
  { qId: "mc6", type: "mc", points: 1, answer: "C", explain: "Substitusi syarat awal: <code>y(0) = C·e<sup>-2(0)</sup> = C·1 = C</code>. Karena <code>y(0) = 5</code>, maka <code>C = 5</code>. Solusi khusus: <code>y = 5·e<sup>-2x</sup></code>." },
  { qId: "mc7", type: "mc", points: 1, answer: "B", explain: "Half-life T<sub>1/2</sub> adalah waktu untuk <code>N(T) = N₀/2</code>. Substitusi: <code>N₀/2 = N₀·e<sup>-λT</sup></code> → <code>1/2 = e<sup>-λT</sup></code> → <code>-λT = ln(1/2) = -ln(2)</code> → <strong>T<sub>1/2</sub> = ln(2)/λ</strong>. Untuk Carbon-14 dengan T<sub>1/2</sub> = 5730 tahun, λ = ln(2)/5730 ≈ 0.000121." },
  { qId: "mc8", type: "mc", points: 1, answer: "A", explain: "Verifikasi: <code>y = e<sup>x²</sup></code> → <code>y' = 2x·e<sup>x²</sup> = 2x·y</code> ✓ memenuhi PD. Cek IC: <code>y(0) = e⁰ = 1</code> ✓ memenuhi syarat awal. Kedua-duanya cocok → solusi terbukti." },
  { qId: "mc9", type: "mc", points: 1, answer: "A", explain: "PD logistik <code>dN/dt = rN(1-N/K)</code> ada produk N dan (1-N/K) — semua fungsi N. PD juga punya implicit faktor t (1 = fungsi t konstan). Maka pisahkan: <code>dN/[N(1-N/K)] = r dt</code>. Setelah dekomposisi pecahan parsial, integrasikan." },
  { qId: "mc10", type: "mc", points: 1, answer: "B", explain: "PD orde 1 = 1× integrasi → <strong>1 konstanta sembarang</strong>. Untuk solusi unik, butuh 1 syarat awal (y(x₀) = y₀). Aturan umum: PD orde-n → n konstanta → n syarat." }
];

const COMP_EZ = [
  { qId: "c1", type: "comp", points: 2, answer: 1, tolerance: 0.001, explain: "PD <code>dy/dx = 2xy</code> dapat ditulis sebagai <code>(2x) · y</code> = produk fungsi-x dan fungsi-y → <strong>variabel terpisah</strong> (kode 1)." },
  { qId: "c2", type: "comp", points: 2, answer: 5, tolerance: 0.005, explain: "Substitusi <code>y(0) = 5</code> ke <code>y = C·e<sup>-2x</sup></code>: <code>C·e⁰ = C = 5</code>." },
  { qId: "c3", type: "comp", points: 2, answer: 2.7183, tolerance: 0.05, explain: "Solusi <code>y = e<sup>x²</sup></code>. <code>y(1) = e¹ ≈ 2.7183</code> (= bilangan Euler)." },
  { qId: "c4", type: "comp", points: 2, answer: 0.6767, tolerance: 0.05, explain: "Solusi <code>y = 5·e<sup>-2x</sup></code>. <code>y(1) = 5·e<sup>-2</sup> ≈ 0.6767</code>." },
  { qId: "c5", type: "comp", points: 2, answer: 49.43, tolerance: 0.5, explain: "Newton Cooling: <code>T(10) = 20 + 80·e<sup>-1</sup> ≈ 20 + 29.43 = 49.43°C</code>. Bola tembaga turun dari 100°C ke ~49°C dalam 10 menit." },
  { qId: "c6", type: "comp", points: 2, answer: 0.000121, tolerance: 0.0001, explain: "<code>λ = ln(2)/5730 ≈ 0.000121</code> per tahun. Konstanta peluruhan Carbon-14." },
  { qId: "c7", type: "comp", points: 2, answer: 6, tolerance: 0.05, explain: "PD <code>dy/y = dx/x</code> → <code>ln|y| = ln|x| + C → y = C₁x</code>. IC <code>y(1)=2 → C₁=2</code>. <code>y(3) = 2·3 = 6</code>." },
  { qId: "c8", type: "comp", points: 2, answer: 2, tolerance: 0.05, explain: "PD <code>dy/dx = e<sup>x</sup>·y²</code>: <code>dy/y² = e<sup>x</sup>dx → −1/y = e<sup>x</sup> + C</code>. IC y(0)=1 → C=−2. Solusi: <code>y = 1/(2−e<sup>x</sup>)</code>. <code>y(ln 1.5) = 1/(2−1.5) = 2</code>." },
  { qId: "c9", type: "comp", points: 2, answer: 1, tolerance: 0.05, explain: "PD <code>dy/(1+y²) = dx → arctan(y) = x + C</code>. IC y(0)=0 → C=0 → <code>y = tan(x)</code>. <code>y(π/4) = tan(π/4) = 1</code>." },
  { qId: "c10", type: "comp", points: 2, answer: 80, tolerance: 0.05, explain: "Newton Cooling amplitudo = <code>T₀ − T<sub>m</sub> = 100 − 20 = 80</code>. Inilah jumlah suhu yang akan \"diturunkan\" dari T₀ menuju T<sub>m</sub> secara eksponensial." }
];

const COMP_HARD = [
  { qId: "c11", type: "comp", points: 4, answer: 2.7183, tolerance: 0.05, allowPartial: true, partialPoints: 1, explain: "SymPy <code>dsolve</code> mengembalikan <code>y(x) = exp(x²)</code>. <code>y(1) = exp(1) ≈ 2.7183</code>. Verifikasi simbolik dari Contoh 2.1." },
  { qId: "c12", type: "comp", points: 4, answer: 30.84, tolerance: 0.5, allowPartial: true, partialPoints: 1, explain: "scipy.solve_ivp dengan <code>def f(t,T): return -0.1*(T[0]-20)</code>. <code>sol.sol(20)[0] ≈ 30.84°C</code>. Setelah 20 menit, bola tembaga sudah dekat sekali dengan suhu ruangan (20°C), tapi belum sama persis." },
  { qId: "c13", type: "comp", points: 4, answer: 0.881, tolerance: 0.05, allowPartial: true, partialPoints: 1, explain: "PD logistik <code>y' = y(1-y)</code>, IC y(0)=0.5, solusi sigmoid <code>y = 1/(1+e<sup>-x</sup>)</code>. <code>y(2) = 1/(1+e<sup>-2</sup>) = 1/(1+0.135) ≈ 0.881</code>. Sigmoid mendekati 1 (asimptot atas)." },
  { qId: "c14", type: "comp", points: 4, answer: 25, tolerance: 0.5, allowPartial: true, partialPoints: 1, explain: "<code>N(t)/N₀ = e<sup>-λt</sup></code> dengan <code>λt = ln(2)·(t/T<sub>1/2</sub>)</code>. Untuk t=2·T<sub>1/2</sub>, <code>N/N₀ = e<sup>-2 ln(2)</sup> = 1/4 = 25%</code>. Setelah 2 half-lives, hanya seperempat material radioaktif tersisa." },
  { qId: "c15", type: "comp", points: 4, answer: 995.5, tolerance: 5, allowPartial: true, partialPoints: 1, explain: "scipy.solve_ivp dengan <code>def f(t,N): return 0.5*N[0]*(1 - N[0]/1000)</code>. <code>sol.sol(20)[0] ≈ 995.5</code>. Populasi awal 10 mendekati K=1000 setelah 20 unit waktu. Pertumbuhan eksponensial cepat di awal, melambat di akhir." }
];

const MODUL_QUESTIONS = [...MC, ...COMP_EZ, ...COMP_HARD];

const SUMMARY = {
  "modulId": "math4-modul-2",
  "totalSoal": 25,
  "totalPoin": 50,
  "breakdown": {
    "mc": 10,
    "compEz": 10,
    "compHard": 5
  }
};

module.exports = { MODUL_QUESTIONS, SUMMARY };
