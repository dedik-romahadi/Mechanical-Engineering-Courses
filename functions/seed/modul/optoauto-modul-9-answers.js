/* eslint-disable max-len */
/**
 * optoauto-modul-9 — answer-key metadata.
 *
 * Auto-generated by functions/seed/extract-modul-answers.js dari source HTML.
 * Edit source HTML, lalu re-extract; JANGAN edit file ini manual.
 *
 * Format: MC entries pakai letter answer (A/B/C/D), Comp pakai numeric answer +
 * tolerance. Explain WAJIB ada (modul reveal jawaban setelah submit untuk
 * pembelajaran formative).
 */

const MC = [
  { qId: "mc1", type: "mc", points: 1, answer: "B", explain: "<strong>Syarat orde-pertama (necessary condition)</strong> bagi local minimum: <code>∇f(x*) = 0</code> — disebut <em>titik stasioner</em>. Ini perlu <em>tetapi tidak cukup</em>: titik stasioner bisa berupa minimum, maksimum, atau saddle point. Untuk konfirmasi minimum, cek <em>second-order condition</em>: <code>∇²f(x*) ⪰ 0</code> (Hessian semi-definit positif). (A) salah — nilai f(x*) bisa apa saja. (C) salah — Hessian semi-definit, bukan nol. (D) salah — variabel x bisa apa saja." },
  { qId: "mc2", type: "mc", points: 1, answer: "B", explain: "<strong>Step size α</strong> menentukan jarak langkah ke arah negative gradient: <code>x_{k+1} = x_k − α·∇f(x_k)</code>. Terlalu besar → overshoot/oscillation/divergen; terlalu kecil → konvergen sangat lambat. <em>Backtracking line search</em> (Armijo rule) menyesuaikan α adaptif: cari α terbesar yang memenuhi <em>sufficient decrease</em>. (A) salah — dimensi ditentukan x. (C) salah — penalty berbeda dari step size. (D) salah — α=1 tidak universal." },
  { qId: "mc3", type: "mc", points: 1, answer: "B", explain: "<strong>Newton's method</strong> menggunakan informasi orde-2 (Hessian) untuk model lokal kuadratik: <code>f(x_k+d) ≈ f(x_k) + ∇fᵀd + ½dᵀHd</code>. Set turunan d = 0: <code>Hd = −∇f → d = −H⁻¹·∇f</code>. <em>Konvergensi kuadratik</em>: error berkurang sebagai kuadrat tiap iterasi (jauh lebih cepat dari linier steepest descent). (A) salah — Newton butuh ∇f dan H. (C) salah — Newton untuk fungsi smooth umum. (D) salah — Newton konvergen dalam region attraction yang baik." },
  { qId: "mc4", type: "mc", points: 1, answer: "B", explain: "<strong>BFGS</strong> (Broyden-Fletcher-Goldfarb-Shanno, 1970) adalah <em>quasi-Newton</em> yang mengaproksimasi Hessian dari pasangan (s, y) = (Δx, Δ∇f). Konvergensi super-linier dengan biaya O(n²) per iterasi (vs Newton O(n³)). Default <code>scipy.optimize.minimize</code> untuk unconstrained. Robust dan sangat cepat untuk n ≤ 1000. (A) salah — BFGS butuh gradient. (C) salah — bukan steepest descent. (D) salah — BFGS untuk NLP, bukan LP simpleks." },
  { qId: "mc5", type: "mc", points: 1, answer: "B", explain: "<strong>Lagrangian function</strong>: <code>L(x, λ, μ) = f(x) + λᵀ·h(x) + μᵀ·g(x)</code> dengan <code>λ ∈ ℝ</code> (multiplier untuk equality), <code>μ ≥ 0</code> (untuk inequality). Stationarity: <code>∇_x L(x*, λ*, μ*) = 0</code> di optimum. Multiplier mewakili <em>shadow price</em> — sensitivity Z optimum terhadap perubahan RHS kendala. (A) salah — tanda harus penjumlahan. (C) tidak masuk akal — Lagrangian aditif. (D) salah — multiplier krusial." },
  { qId: "mc6", type: "mc", points: 1, answer: "B", explain: "<strong>Complementary slackness</strong>: <code>μⱼ · gⱼ(x*) = 0</code> ∀j. Konsekuensi: kendala <em>aktif</em> (binding, gⱼ = 0) bisa punya μⱼ &gt; 0; kendala <em>tidak aktif</em> (gⱼ &lt; 0) harus punya μⱼ = 0. Ini cara natural KKT mengklasifikasi mana kendala \"binding\" tanpa menebak — multiplier μ memberi info itu langsung dari hasil solver. (A) salah — penjumlahan tidak nol. (C) salah — μ ≠ g. (D) salah — μ tidak harus &gt; g." },
  { qId: "mc7", type: "mc", points: 1, answer: "B", explain: "<strong>Penalty method</strong> mengkonversi kendala jadi penalty di objektif: <code>F_ρ(x) = f(x) + ρ·Σ max(0, gⱼ(x))²</code>. Saat ρ kecil, kendala bisa dilanggar; saat ρ → ∞, solusi unconstrained dipaksa mendekati feasible. <em>Trade-off</em>: ρ kecil → solusi tidak akurat; ρ besar → ill-conditioning. Variant: <em>quadratic penalty</em>, <em>augmented Lagrangian</em> (lebih stabil). (A) salah — kendala diolah, bukan dihapus. (C) tidak masuk akal — log transform untuk barrier method. (D) salah — penalty universal untuk LP/NLP." },
  { qId: "mc8", type: "mc", points: 1, answer: "B", explain: "<strong>SLSQP</strong> (Sequential Quadratic Programming) dan <strong>trust-constr</strong> adalah default scipy untuk NLP berkendala umum. SLSQP cocok untuk n &lt; 100; trust-constr lebih robust untuk masalah besar &amp; non-konveks. Keduanya support equality, inequality, bounds. (A) salah — BFGS hanya unconstrained. (C) salah — Nelder-Mead derivative-free, tidak handle constraints langsung di scipy.minimize. (D) salah — CG hanya unconstrained." },
  { qId: "mc9", type: "mc", points: 1, answer: "B", explain: "<strong>Konveksitas</strong> garansi: untuk fungsi konveks, sembarang local minimum = global minimum. Ini sifat fundamental yang membuat NLP konveks <em>tractable</em>. Cek konveksitas: Hessian <code>∇²f(x) ⪰ 0</code> ∀x ∈ domain (semi-definit positif). LP, QP konveks, NLP dengan f &amp; g convex semua punya garansi ini. Untuk non-konveks, gradient bisa stuck di local min — multi-start atau metaheuristik (Pertemuan 11) diperlukan. (A) salah — saddle terjadi di non-konveks. (C) salah — minimum bukan maksimum. (D) salah — ada hubungan kuat untuk konveks." },
  { qId: "mc10", type: "mc", points: 1, answer: "B", explain: "Untuk <em>non-konveks NLP</em> dengan banyak local minimum, gradient-based dari satu titik awal tidak menjamin global. <strong>Multi-start</strong>: jalankan minimize dari banyak x₀ random, ambil hasil terbaik — sederhana, scalable, tidak menjamin tetapi sering cukup di praktik. <strong>Metaheuristik</strong> (GA, PSO, Differential Evolution): population-based, eksplor seluruh space, tetapi lebih lambat. SciPy: <code>scipy.optimize.basinhopping</code>, <code>differential_evolution</code>. (A) salah — BFGS sekali tidak menjamin. (C) tidak masuk akal — formulasi non-konveks tidak bisa dilineralisasi sembarangan. (D) salah — gradient descent stuck di lokal optimum." }
];

const COMP_EZ = [
  { qId: "c1", type: "comp", points: 2, answer: 4, tolerance: 0.01, explain: "Min <code>f(x) = (x − 4)²</code>: solusi analitik <code>f'(x) = 2(x−4) = 0 → x* = 4</code>. SciPy BFGS dari x0 = 0 konvergen dalam ~5 iterasi: <code>res = minimize(lambda x: (x[0]-4)**2, x0=[0], method='BFGS'); print(res.x[0])</code> = <strong>4.0</strong>. Cek <code>res.success = True</code>, <code>res.fun ≈ 0</code> (numerik mendekati nol)." },
  { qId: "c2", type: "comp", points: 2, answer: 5.6569, tolerance: 0.01, explain: "Gradient analytic: <code>∇f(x,y) = (2(x−3), 4(y−2))</code>. Di (1,1): <code>∇f = (−4, −4)</code>. Norma Euclidean: <code>‖∇f‖ = √(16 + 16) = √32 ≈ <strong>5.6569</strong></code>. Gradient menunjukkan arah peningkatan tercepat — di sini menunjuk dari (1,1) menjauhi optimum (3,2). Negasi (−∇f) adalah arah descent." },
  { qId: "c3", type: "comp", points: 2, answer: 0, tolerance: 0.001, explain: "Min f(x,y) = x² + 2y². Solusi: <code>∇f = (2x, 4y) = 0 → x*=y*=0, f*=0</code>. SciPy BFGS dari [3,2] konvergen dalam ~3 iterasi karena f quadratic positive-definite (Hessian diagonal [2, 4] ≻ 0). Hasil <code>res.fun</code> ≈ <strong>0</strong> (mungkin 1e-10 karena floating point). Ini contoh fungsi konveks tegas — pasti unique global minimum." },
  { qId: "c4", type: "comp", points: 2, answer: 2, tolerance: 0.01, explain: "NLP berkendala: <code>min x² + y² s.t. x + y = 4</code>. Lagrangian: <code>L = x² + y² + λ(x+y−4)</code>. KKT: <code>∂L/∂x = 2x + λ = 0, ∂L/∂y = 2y + λ = 0</code> → <code>x = y</code>. Substitusi ke kendala: <code>2x = 4 → x = y = 2</code>, λ = −4. Pakai SLSQP: <code>cons = {'type':'eq', 'fun': lambda x: x[0]+x[1]-4}; res = minimize(...); res.x[0]</code> = <strong>2.0</strong>." },
  { qId: "c5", type: "comp", points: 2, answer: -4, tolerance: 0.01, explain: "Lagrange multiplier untuk masalah C4: dari <code>2x + λ = 0</code> dan <code>x = 2</code> → <code>λ = -2x = <strong>-4</strong></code>. Arti fisik: jika kendala diubah dari <code>x+y=4</code> ke <code>x+y=4+ε</code> (ε kecil), nilai f optimum akan turun sebesar |λ|·ε = 4·ε. SLSQP tidak langsung kembalikan multiplier; pakai <code>method='trust-constr'</code> dengan <code>res.v</code> untuk Lagrange multiplier eksplisit. Atau hitung manual seperti hint." },
  { qId: "c6", type: "comp", points: 2, answer: 2, tolerance: 0.01, explain: "Gradient descent 1 iter: <code>f(x) = x²</code>, <code>f'(x) = 2x</code>. Di x₀ = 5: <code>f'(5) = 10</code>. Update: <code>x₁ = x₀ − α·f'(x₀) = 5 − 0.3·10 = 5 − 3 = <strong>2.0</strong></code>. Iterasi berikutnya: x₂ = 2 − 0.3·4 = 0.8, x₃ = 0.32, ... → konvergen ke 0. Step size α = 0.3 sangat agresif; dengan α = 0.5, x₁ = 0 (langsung optimum karena fungsi quadratic + α optimal exact)." },
  { qId: "c7", type: "comp", points: 2, answer: 2.16, tolerance: 0.01, explain: "Newton step untuk <code>f(x) = x⁴ − 4x² + 5</code>: <code>f'(x) = 4x³ − 8x</code>, <code>f''(x) = 12x² − 8</code>. Di x₀ = 3: <code>f'(3) = 108 − 24 = 84</code>; <code>f''(3) = 108 − 8 = 100</code>. Newton update: <code>x₁ = 3 − 84/100 = <strong>2.16</strong></code>. Iterasi berikutnya akan mendekati x* = √2 ≈ 1.414 (salah satu dari dua minimum global). Newton konvergensi kuadratik di domain attraction." },
  { qId: "c8", type: "comp", points: 2, answer: 16, tolerance: 0.01, explain: "Validasi konveksitas <code>f(x) = x⁴ + 2x² + 1</code>: <code>f'(x) = 4x³ + 4x</code>, <code>f''(x) = 12x² + 4</code>. Di x = 1: <code>f''(1) = 12 + 4 = <strong>16</strong></code>. Karena <code>f''(x) = 12x² + 4 ≥ 4 &gt; 0</code> ∀x, fungsi ini <em>strictly convex</em>. Konsekuensi: ada <em>unique</em> global minimum (di x = 0, f(0) = 1), dan sembarang algoritma gradient akan konvergen ke sana." },
  { qId: "c9", type: "comp", points: 2, answer: 1, tolerance: 0.01, explain: "NLP dengan bounds: <code>min (x−2)² + (y−3)² s.t. 0≤x≤1, 0≤y≤5</code>. Tanpa kendala, optimum di (2, 3). Tapi karena kendala x ≤ 1, projection ke boundary: x = 1 (upper bound binding), y = 3 (interior). L-BFGS-B menangani bounds langsung tanpa conversion. <code>res = minimize(..., method='L-BFGS-B', bounds=[(0,1),(0,5)]); res.x[0]</code> = <strong>1.0</strong>." },
  { qId: "c10", type: "comp", points: 2, answer: 27.25, tolerance: 0.05, explain: "Penalty function: <code>F(x) = x² + ρ·max(0, 2−x)²</code>. Di x = 1.5 dengan ρ = 100: <code>x² = 2.25</code>, <code>max(0, 2−1.5) = 0.5</code>, <code>penalty = 100·(0.5)² = 25</code>. Total: <code>F(1.5) = 2.25 + 25 = <strong>27.25</strong></code>. Saat ρ → ∞, minimum F → x = 2 (constraint binding). Untuk ρ = 100, minimum sekitar x = 1.98 (bukan persis 2 karena finite ρ)." }
];

const COMP_HARD = [
  { qId: "c11", type: "comp", points: 4, answer: 2, tolerance: 0.01, allowPartial: true, partialPoints: 1, explain: "Rosenbrock function: <code>f(x,y) = (1−x)² + 100(y−x²)²</code>. Solusi unique: <code>x* = 1, y* = 1, f* = 0</code>. Test klasik untuk optimasi NLP. SciPy BFGS dari [−1.2, 1.0] (challenging start) konvergen dalam ~25 iterasi. <code>res.x.sum() = 1 + 1 = <strong>2.0</strong></code>. Lembah berbentuk pisang — gradient descent biasa lambat zigzag, tapi BFGS mengatasi dengan Hessian aproksimasi." },
  { qId: "c12", type: "comp", points: 4, answer: 0.5419, tolerance: 0.01, allowPartial: true, partialPoints: 1, explain: "Tangki silinder volume tetap: solusi analytic via Lagrange. Lagrangian <code>L = 2πR² + 2πRH + λ(πR²H − V₀)</code>. KKT: <code>∂L/∂R = 4πR + 2πH + 2λπRH = 0</code>, <code>∂L/∂H = 2πR + λπR² = 0</code> → <code>H = 2R</code> (proporsional!), <code>R = ∛(V₀/2π) ≈ <strong>0.5419 m</strong></code>. SLSQP dari x0=[0.5, 1.0] feasible konvergen cepat. Multiplier λ = shadow price terhadap volume target." },
  { qId: "c13", type: "comp", points: 4, answer: 1.9802, tolerance: 0.05, allowPartial: true, partialPoints: 1, explain: "Penalty method untuk <code>min x² s.t. x ≥ 2</code>: <code>F(x) = x² + ρ·max(0, 2−x)²</code>. Untuk ρ = 100: minimum di <code>x = 200/101 ≈ 1.9802</code> (analytic via dF/dx = 0 di region constraint violated). Saat ρ → ∞: x → 2 (constraint binding). Code: <code>res = minimize(lambda x: x[0]**2 + 100*max(0,2-x[0])**2, x0=[0]); res.x[0]</code> = <strong>1.9802</strong>. Pelajaran: penalty large = numerical issues; gunakan augmented Lagrangian untuk stabilitas." },
  { qId: "c14", type: "comp", points: 4, answer: -11, tolerance: 0.05, allowPartial: true, partialPoints: 1, explain: "Multi-start untuk non-konveks: <code>f(x) = x⁴ − 8x² + 5</code>. <code>f'(x) = 4x³ − 16x = 4x(x²−4)</code> → titik stasioner: x = 0 (max lokal, f=5), x = ±2 (min global, f = 16−32+5 = <strong>-11</strong>). Multi-start dari random x0 di [−5, 5]: beberapa run mendarat di x = +2, beberapa di x = −2, semua konvergen ke f = −11. Kunci: <em>min(fs)</em> setelah 10 run = global minimum. Tanpa multi-start, x0 = 0.5 stuck di lokal min x = 2 saja." },
  { qId: "c15", type: "comp", points: 4, answer: 41, tolerance: 5, allowPartial: true, partialPoints: 1, explain: "Pegas coil engineering design — masalah klasik Rao \"Engineering Optimization\". Solusi optimal sekitar <strong>m ≈ 0.041 kg = 41 g</strong>. Tegangan binding (τ_max = τ_y = 600 MPa), defleksi binding (δ = 25 mm), spacing slack. d_opt ≈ 5–7 mm, N_opt ≈ 9–12, L_opt ≈ 80–100 mm. Variasi acceptable ±5 g tergantung initial guess dan numerical tolerance. Wahl factor di formula tegangan akan menambah margin 10–15%." }
];

const MODUL_QUESTIONS = [...MC, ...COMP_EZ, ...COMP_HARD];

const SUMMARY = {
  "modulId": "optoauto-modul-9",
  "totalSoal": 25,
  "totalPoin": 50,
  "breakdown": {
    "mc": 10,
    "compEz": 10,
    "compHard": 5
  }
};

module.exports = { MODUL_QUESTIONS, SUMMARY };
