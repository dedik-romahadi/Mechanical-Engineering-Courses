/* eslint-disable max-len */
/**
 * optoauto-modul-8 — answer-key metadata.
 *
 * Auto-generated by functions/seed/extract-modul-answers.js dari source HTML.
 * Edit source HTML, lalu re-extract; JANGAN edit file ini manual.
 *
 * Format: MC entries pakai letter answer (A/B/C/D), Comp pakai numeric answer +
 * tolerance. Explain WAJIB ada (modul reveal jawaban setelah submit untuk
 * pembelajaran formative).
 */

const MC = [
  { qId: "mc1", type: "mc", points: 1, answer: "B", explain: "<strong>Linear Programming</strong> mengasumsikan: (1) <em>linearitas</em> di fungsi objektif maupun kendala, (2) <em>divisibility</em> — variabel bersifat kontinu (boleh pecahan); (3) <em>certainty</em> — parameter c, A, b deterministik; (4) <em>non-negativity</em> default <code>x ≥ 0</code> (kecuali secara eksplisit dibuat free). (A) salah — fungsi kuadratik melanggar linearitas, gunakan QP. (C) salah — variabel integer butuh MILP. (D) salah — LP modern menyelesaikan jutaan variabel via HiGHS/Gurobi/CPLEX." },
  { qId: "mc2", type: "mc", points: 1, answer: "B", explain: "<strong>Teorema Dasar LP</strong> (Dantzig 1947): jika LP punya solusi optimal terbatas, <em>setidaknya satu vertex (titik sudut)</em> daerah feasibel adalah solusi optimal. Konsekuensi: pencarian dari titik tak-hingga di poligon konveks dapat direduksi menjadi enumerasi vertex (jumlahnya finite). Inilah yang membuat LP <em>tractable</em> via algoritma Simpleks yang loncat dari vertex ke vertex. (A) salah — titik tengah biasanya tidak optimal. (C) salah — origin hanya optimal jika cᵀx = 0 di sana minimum/maksimum. (D) tidak ada hubungannya dengan luasan." },
  { qId: "mc3", type: "mc", points: 1, answer: "B", explain: "<strong>Slack variable</strong> <code>sᵢ ≥ 0</code> mengkonversi kendala <code>aᵢᵀx ≤ bᵢ</code> menjadi <em>equality</em> <code>aᵢᵀx + sᵢ = bᵢ</code>, dengan <code>sᵢ</code> = \"sumber daya yang tidak terpakai\". Ini diperlukan agar tableau Simpleks bisa dibentuk dengan kolom identity (basic feasible solution awal). Untuk kendala <code>≥</code>, kurangi <em>surplus variable</em>. Slack di vertex optimal: <code>sᵢ = 0</code> berarti kendala <em>binding</em> (sumber daya habis). (A) tidak ada hubungan dengan probabilitas. (C) salah, slack tidak mengubah linieritas. (D) menyesatkan." },
  { qId: "mc4", type: "mc", points: 1, answer: "B", explain: "<code>scipy.optimize.linprog</code> selalu MEMINIMUMKAN: <code>min cᵀx</code>. Untuk MAKSIMUMKAN, kirim <code>c_negate = -c_orig</code>, lalu nilai Z optimum dikembalikan dengan <code>Z_max = -result.fun</code>. Variabel x optimal tidak perlu dinegasikan: <code>x* = result.x</code> tetap valid. (A) salah — tidak ada parameter direction='max'. (C) salah — bisa lewat trick negasi. (D) salah — A_ub, b_ub tidak dinegasikan untuk swap min↔max." },
  { qId: "mc5", type: "mc", points: 1, answer: "B", explain: "<strong>Status \"unbounded\"</strong> berarti daerah feasibel terbentang tanpa batas pada arah peningkatan Z (gradien c). Z bisa naik ∞ — jadi tidak ada solusi optimal terbatas. Penyebab umum: <em>kurang kendala fisik</em> (mis. lupa kapasitas mesin, lupa demand maksimal). Solusi: tambah kendala upper bound yang masuk akal. (A) salah — itu adalah status \"optimal\". (C) salah — itu status \"infeasible\". (D) salah — solver modern langsung kasih status, bukan tunggu iterasi." },
  { qId: "mc6", type: "mc", points: 1, answer: "B", explain: "<strong>Daerah feasibel</strong> LP = <em>polihedron konveks</em> (poligon di 2D, polihedron di nD), terbentuk dari irisan setengah-bidang dari tiap kendala <code>aᵢᵀx ≤ bᵢ</code>. Konveksitas adalah karakteristik fundamental yang membuat <em>maksimum/minimum lokal = global</em> — tidak ada perangkap optimum lokal. (A) salah — lingkaran adalah set non-poligonal. (C) salah — daerah feasibel kadang bounded, kadang unbounded, tergantung kendala. (D) salah — bukan fraktal." },
  { qId: "mc7", type: "mc", points: 1, answer: "B", explain: "<strong>Algoritma Simpleks</strong> (Dantzig 1947): mulai dari vertex initial (biasanya origin x=0 jika feasible), pilih NBV yang masuk basis berdasarkan <em>reduced cost</em> (Dantzig's rule: paling positif untuk max), lalu pilih BV keluar via <em>minimum ratio test</em>. Pivot Gauss-Jordan, lompat ke vertex tetangga dengan Z lebih baik. Ulangi sampai tidak ada NBV yang menaikkan Z → optimal. (A) salah — tidak periksa semua titik. (C) tidak masuk akal. (D) salah — Simpleks bukan gradient descent kontinu." },
  { qId: "mc8", type: "mc", points: 1, answer: "B", explain: "<strong>Shadow price</strong> (= dual variable yᵢ untuk kendala i) = perubahan marjinal Z optimal jika RHS bᵢ ditambah 1 unit. Misal pabrik LP C2 dengan kendala CNC ≤ 60 dan dual y_CNC = 18, berarti tambahan 1 jam CNC akan menaikkan profit Rp 18k. Insight bisnis: <em>kendala dengan dual tertinggi adalah bottleneck — ROI investasi terbesar</em>. (A) salah — biaya total bukan dual. (C) salah — tidak ada hubungan probabilitas. (D) salah — shadow price bukan koefisien c." },
  { qId: "mc9", type: "mc", points: 1, answer: "B", explain: "<code>scipy.optimize.linprog</code> hanya menerima bentuk <code>A_ub @ x ≤ b_ub</code>. Untuk kendala <code>≥</code>, negasikan kedua sisi: <code>aᵀx ≥ b</code> ↔ <code>−aᵀx ≤ −b</code>. Manuver ini membalik tanda inequality dan tidak mengubah daerah feasibel. (A) salah — equality berbeda dari inequality, beda daerah feasibel. (C) tidak masuk akal — LP tidak menerima variabel kuadratik. (D) salah — scipy tidak menerima ≥ langsung." },
  { qId: "mc10", type: "mc", points: 1, answer: "B", explain: "LP murni mengasumsikan <em>divisibility</em> — variabel kontinu (3.7 unit valid). Untuk masalah dengan kebutuhan integer, gunakan <strong>Mixed-Integer Linear Programming (MILP)</strong> via <code>scipy.optimize.milp</code> (scipy ≥ 1.9) atau library specialized (PuLP/Pyomo + Gurobi/CPLEX). MILP solver memakai <em>branch-and-bound</em> dengan LP relaxation — lebih lambat tetapi memberi solusi integer terbukti optimal. <em>Pembulatan LP biasa</em> (A) sering infeasible (mis. round-up melanggar kapasitas) atau sub-optimal (round-down kehilangan profit). (C) tidak relevan untuk kasus integer linear. (D) tidak masuk akal." }
];

const COMP_EZ = [
  { qId: "c1", type: "comp", points: 2, answer: 25, tolerance: 0.05, explain: "LP 1D: <code>max Z = 5x</code> s.t. <code>2x ≤ 10</code>, <code>x ≥ 0</code>. Solusi: <code>x* = 5</code> (kendala binding), <code>Z* = 5·5 = <strong>25</strong></code>. Implementasi: <code>linprog(c=[-5], A_ub=[[2]], b_ub=[10], bounds=[(0,None)])</code>; print <code>-res.fun</code>. Ini contoh paling sederhana — vertex optimal di endpoint kanan rentang feasibel." },
  { qId: "c2", type: "comp", points: 2, answer: 1400, tolerance: 1, explain: "Pabrik bracket 2-produk: <code>max Z = 40x_A + 30x_B</code> s.t. <code>2x_A + x_B ≤ 60</code>, <code>x_A + 3x_B ≤ 80</code>, <code>x ≥ 0</code>. Vertex feasibel: (0,0), (30,0), (20,20), (0,80/3). Z di tiap vertex: 0, 1200, 1400, 800. Optimal di (20,20) → <strong>Z = 1400</strong>. Kedua kendala binding di vertex optimal — tidak ada slack, ini bottleneck simultan." },
  { qId: "c3", type: "comp", points: 2, answer: 20, tolerance: 0.05, explain: "Untuk LP yang sama dengan C2, x_A optimal = <strong>20</strong>. Vertex optimal di perpotongan kendala CNC + Welding: solve sistem <code>2x_A + x_B = 60</code> dan <code>x_A + 3x_B = 80</code>. Kalikan persamaan-1 dengan 3: <code>6x_A + 3x_B = 180</code>; kurangi persamaan-2: <code>5x_A = 100</code> → <code>x_A = 20</code>. Lalu <code>x_B = 60 − 2·20 = 20</code>. Verifikasi: kedua kendala memang habis terpakai." },
  { qId: "c4", type: "comp", points: 2, answer: 17, tolerance: 0.05, explain: "LP minimisasi diet: <code>min Z = 2x₁ + 3x₂</code> s.t. <code>x₁ + 2x₂ ≥ 10</code>, <code>3x₁ + x₂ ≥ 15</code>, <code>x ≥ 0</code>. Konversi ≥ jadi ≤ via negasi: <code>A_ub = [[-1,-2],[-3,-1]], b_ub = [-10,-15]</code>. <code>c = [2,3]</code> langsung (sudah min). Solusi optimal di vertex (4,3): <code>Z = 2·4 + 3·3 = <strong>17</strong></code>. Kedua kendala binding." },
  { qId: "c5", type: "comp", points: 2, answer: 733.33, tolerance: 0.5, explain: "LP 3 variabel klasik (Hillier-Lieberman): <code>max Z = 10x₁ + 6x₂ + 4x₃</code> s.t. 3 kendala. Vertex optimal di (33⅓, 66⅔, 0) — kendala 1 dan 3 binding, x₃ = 0. <code>Z = 10·33.33 + 6·66.67 + 4·0 = 333.33 + 400.00 ≈ <strong>733.33</strong></code>. Salah satu kendala (kendala 2: 10x₁+4x₂+5x₃ ≤ 600) tidak binding (slack &gt; 0)." },
  { qId: "c6", type: "comp", points: 2, answer: 0, tolerance: 0.05, explain: "Slack kendala CNC di vertex optimal C2: <code>s_CNC = 60 − (2·20 + 1·20) = 60 − 60 = <strong>0</strong></code>. Slack = 0 berarti kendala <em>binding</em> (sumber daya CNC sepenuhnya terpakai). Bisa juga didapat lewat <code>res.ineqlin.residual[0]</code>. Insight: kendala binding adalah <em>bottleneck</em> — investasi tambah kapasitas di sini akan menambah Z (sesuai shadow price)." },
  { qId: "c7", type: "comp", points: 2, answer: 18, tolerance: 0.1, explain: "Shadow price kendala CNC pada LP C2: solve sistem dual <code>2y₁+y₂=40, y₁+3y₂=30</code> → <code>y₁=18, y₂=4</code>. Jadi shadow_CNC = <strong>18</strong> (Rp 18k/jam). Verifikasi: 60·18 + 80·4 = 1080+320 = 1400 = Z primal ✓ (strong duality). Artinya: tambahan 1 jam CNC → profit naik Rp 18k. Welding shadow lebih kecil (4) — CNC adalah bottleneck dominan di LP ini." },
  { qId: "c8", type: "comp", points: 2, answer: 10, tolerance: 0.05, explain: "LP equality: <code>min Z = x₁ + 2x₂</code> s.t. <code>x₁ + x₂ = 10</code>, <code>x ≥ 0</code>. Karena ingin minimum dan koef x₁ &lt; koef x₂ (1 vs 2), maksimal-kan x₁ dan minimum-kan x₂. Kendala equality: x₁ + x₂ = 10, jadi optimal x₁ = 10, x₂ = 0 → <code>Z = <strong>10</strong></code>. Pakai parameter <code>A_eq, b_eq</code> di <code>linprog</code> untuk equality kendala." },
  { qId: "c9", type: "comp", points: 2, answer: 11, tolerance: 0.05, explain: "Vertex enumerasi manual: untuk LP <code>max Z = 3x₁ + 2x₂</code> s.t. <code>x₁ + x₂ ≤ 4, x₁ ≤ 3, x ≥ 0</code>, ada 4 vertex feasibel. Z di tiap vertex: (0,0)→0, (3,0)→9, (3,1)→11, (0,4)→8. Maksimum = <strong>11</strong> di vertex (3,1). Loop Python: <code>vertices = [(0,0),(3,0),(3,1),(0,4)]; Z_max = max(3*v[0]+2*v[1] for v in vertices)</code>. Cara kerja brute-force ini valid untuk LP kecil tapi tidak scalable ke n besar." },
  { qId: "c10", type: "comp", points: 2, answer: 220, tolerance: 0.5, explain: "LP dengan bounds: <code>max Z = 5x₁ + 4x₂</code> s.t. <code>x₁ + x₂ ≤ 50</code>, <code>0 ≤ x₁ ≤ 20</code>, <code>0 ≤ x₂ ≤ 35</code>. Karena x₁ profit lebih besar (5 vs 4), maksimal-kan x₁ → x₁ = 20 (upper bound binding). Lalu x₂ = min(35, 50-20) = 30. <code>Z = 5·20 + 4·30 = 100 + 120 = <strong>220</strong></code>. Pakai <code>bounds=[(0,20),(0,35)]</code> di linprog. Bounds lebih efisien daripada menulis kendala umum." }
];

const COMP_HARD = [
  { qId: "c11", type: "comp", points: 4, answer: 2737.5, tolerance: 5, allowPartial: true, partialPoints: 1, explain: "LP 4-produk dengan 6 kendala. Solusi optimal: x = (0, 18.75, 0, 30) — B1 dan B3 tidak diproduksi, B4 di kendala upper bound (30). <code>Z = 30·0 + 50·18.75 + 40·0 + 60·30 = 0 + 937.5 + 0 + 1800 = <strong>2737.5</strong></code>. Welding paling binding (kendala dual tertinggi). Jangan lupa kendala kelima <code>−x₂ ≤ −5</code> (negasi dari x₂ ≥ 5)." },
  { qId: "c12", type: "comp", points: 4, answer: 650, tolerance: 1, allowPartial: true, partialPoints: 1, explain: "Transportation Problem balanced (Σsupply = Σdemand = 70). Flatten: c = [8,6,10,9,12,13]. Supply: <code>x_11+x_12+x_13 ≤ 30</code>, <code>x_21+x_22+x_23 ≤ 40</code>. Demand: <code>x_11+x_21 = 20</code>, <code>x_12+x_22 = 25</code>, <code>x_13+x_23 = 25</code>. Solusi optimal: x_11=5, x_12=25, x_13=0, x_21=15, x_22=0, x_23=25. Total = 8·5 + 6·25 + 10·0 + 9·15 + 12·0 + 13·25 = 40+150+0+135+0+325 = <strong>650</strong>." },
  { qId: "c13", type: "comp", points: 4, answer: 1000, tolerance: 1, allowPartial: true, partialPoints: 1, explain: "Steel alloy blending: <code>min Z = 10x₁ + 8x₂ + 12x₃</code> s.t. <code>x₁+x₂+x₃ = 100</code> (total weight), <code>0.5x₁+0.3x₂+0.7x₃ ≥ 50</code> (50 kg karbon = 0.5% × 100 kg). Karena material 1 (Rp 10/kg, 0.5% C) tepat memenuhi target karbon 0.5% per 100 kg, solusi optimal: x₁ = 100, x₂ = x₃ = 0 → <code>Z = 10·100 = <strong>1000</strong></code>. Kalau target karbon lebih ketat (mis. ≥ 0.6%), butuh blend dengan x₃ (paling kaya karbon)." },
  { qId: "c14", type: "comp", points: 4, answer: 18.67, tolerance: 0.1, allowPartial: true, partialPoints: 1, explain: "Vertex enumerasi LP 2D: cari semua perpotongan dari 4 garis (2 kendala + 2 sumbu). 6 pasangan menghasilkan calon vertex; filter feasibel (semua kendala terpenuhi + non-negative). Vertex feasibel: (0,0), (4,0), (0,4), (8/3, 8/3). Z = 4x₁+3x₂ di vertex: 0, 16, 12, 56/3 ≈ <strong>18.67</strong>. Maksimum di (8/3, 8/3) = perpotongan dua kendala. Pakai <code>np.linalg.solve(A_pair, b_pair)</code> untuk tiap pasangan baris kendala." },
  { qId: "c15", type: "comp", points: 4, answer: 90, tolerance: 0.5, allowPartial: true, partialPoints: 1, explain: "Sensitivity analysis: solve LP awal (b_CNC = 60) → Z_lama = 1400 di vertex (20, 20). Solve LP baru (b_CNC = 65, +5 jam) → vertex bergeser ke (23, 19), Z_baru = 40·23 + 30·19 = 920+570 = 1490. ΔZ = <strong>90</strong>. Cocok dengan prediksi: shadow_CNC · Δb = 18 · 5 = 90 ✓. Strong duality di sensitivity range bekerja sempurna. Jika Δb di luar range (mis. Δb = 50 jam), basis bisa berubah dan ΔZ tidak persis = shadow·Δb — perlu re-solve." }
];

const MODUL_QUESTIONS = [...MC, ...COMP_EZ, ...COMP_HARD];

const SUMMARY = {
  "modulId": "optoauto-modul-8",
  "totalSoal": 25,
  "totalPoin": 50,
  "breakdown": {
    "mc": 10,
    "compEz": 10,
    "compHard": 5
  }
};

module.exports = { MODUL_QUESTIONS, SUMMARY };
