/* eslint-disable max-len */
/**
 * math4-modul-3 — answer-key metadata.
 *
 * Auto-generated by functions/seed/extract-modul-answers.js dari source HTML.
 * Edit source HTML, lalu re-extract; JANGAN edit file ini manual.
 *
 * Format: MC entries pakai letter answer (A/B/C/D), Comp pakai numeric answer +
 * tolerance. Explain WAJIB ada (modul reveal jawaban setelah submit untuk
 * pembelajaran formative).
 */

const MC = [
  { qId: "mc1", type: "mc", points: 1, answer: "B", explain: "Cek <code>f(αx, αy) = 4α²x² − 3α²y² = α²(4x²−3y²) = α² f(x,y)</code> → derajad 2 ✓. Opsi A salah karena suku <code>x²</code> derajad 2 dan <code>y</code> derajad 1 (tidak konsisten). Opsi C dan D juga punya derajat tidak konsisten." },
  { qId: "mc2", type: "mc", points: 1, answer: "A", explain: "Definisi PD homogen: <code>g</code> dan <code>h</code> keduanya fungsi homogen <strong>derajat sama</strong>. Jangan tertukar dengan \"PD homogen vs non-homogen\" pada PD linier orde tinggi (yang merujuk pada r(x) = 0) — ini konsep berbeda. Opsi C salah karena merujuk pada konsep yang lain." },
  { qId: "mc3", type: "mc", points: 1, answer: "C", explain: "Opsi C: <code>g = x</code> derajad 1, <code>h = y²</code> derajad 2 — derajat <strong>tidak sama</strong> → bukan homogen. Opsi A &amp; B keduanya homogen derajad 2 dan 3 (dari Contoh 3.1 dan 3.2). Opsi D keduanya derajad 1." },
  { qId: "mc4", type: "mc", points: 1, answer: "B", explain: "Untuk Contoh 3.1, koefisien <code>x</code> dominan jadi pakai <code>y = ux</code>. Aturan produk pada <code>y = ux</code> memberi <code>dy = u dx + x du</code> (bukan <code>u dx</code> saja). Substitusi yang lupa term <code>x du</code> akan kehilangan informasi diferensial." },
  { qId: "mc5", type: "mc", points: 1, answer: "B", explain: "Contoh 3.2 punya koefisien <code>−(x³ + y³)</code> dengan suku <code>y³</code> dominan di <code>dy</code> → pilih <code>x = vy</code>. Hasil aljabar jauh lebih pendek. Pemilihan substitusi adalah strategi: kedua jalur valid, tapi yang dipilih dengan benar punya integral lebih sederhana." },
  { qId: "mc6", type: "mc", points: 1, answer: "B", explain: "Syarat eksak: turunan parsial <strong>silang</strong> harus sama → <code>∂M/∂y = ∂N/∂x</code>. Ini berasal dari teorema Clairaut: jika fungsi potensial <code>F</code> ada dengan <code>F<sub>x</sub> = M</code> dan <code>F<sub>y</sub> = N</code>, maka <code>F<sub>xy</sub> = F<sub>yx</sub></code> menjamin <code>M<sub>y</sub> = N<sub>x</sub></code>. Opsi A keliru orientasi turunan parsial." },
  { qId: "mc7", type: "mc", points: 1, answer: "A", explain: "<code>M = 3x² + 2xy³</code>. Diferensialkan terhadap <code>y</code> (perlakukan <code>x</code> konstan): <code>∂M/∂y = ∂(3x²)/∂y + ∂(2xy³)/∂y = 0 + 2x · 3y² = <strong>6xy²</strong></code>. Untuk verifikasi eksak, <code>N<sub>x</sub></code> juga harus <code>6xy²</code> (yang memang benar)." },
  { qId: "mc8", type: "mc", points: 1, answer: "C", explain: "Ikuti 4 langkah: (1) cek <code>M<sub>y</sub> = N<sub>x</sub> = 6xy²</code> ✓. (2) <code>F = ∫M dx = x³ + x²y³ + g(y)</code>. (3) <code>F<sub>y</sub> = 3x²y² + g'(y) = N = 3x²y² − 2y³</code> → <code>g'(y) = −2y³</code> → <code>g(y) = −y⁴/2</code>. (4) Solusi: <code>x³ + x²y³ − ½y⁴ = c</code>." },
  { qId: "mc9", type: "mc", points: 1, answer: "A", explain: "Substitusi <code>x = 0, y = 2</code> ke solusi umum: <code>0 + e<sup>0·2</sup> + 2² = 0 + 1 + 4 = 5</code>. Jadi <code>c = 5</code> dan solusi khusus: <code>x + e<sup>xy</sup> + y² = 5</code>." },
  { qId: "mc10", type: "mc", points: 1, answer: "A", explain: "Solusi PD eksak berbentuk implisit <code>F(x,y) = c</code> yang merefleksikan kontur fungsi potensial. Contoh: <code>x + e<sup>xy</sup> + y² = 5</code> sulit diuraikan menjadi <code>y = f(x)</code>. Bandingkan dengan PD homogen yang biasanya bisa diuraikan jadi <code>y(x)</code> eksplisit. Ini konsekuensi struktur kontur." }
];

const COMP_EZ = [
  { qId: "c1", type: "comp", points: 2, answer: 2, tolerance: 0.001, explain: "PD <code>y'' + 3y' − 5y = 7x</code>. Turunan tertinggi adalah <code>y''</code> → orde = <strong>2</strong>." },
  { qId: "c2", type: "comp", points: 2, answer: 16, tolerance: 0.05, explain: "PD <code>(y''')² + (y'')³ − y = sin x</code>. Turunan tertinggi <code>y'''</code> dengan pangkat 2 → derajat = <strong>2</strong>." },
  { qId: "c3", type: "comp", points: 2, answer: 0, tolerance: 0.001, explain: "Turunkan: <code>y' = 2exp(2x), y'' = 4exp(2x)</code>. Substitusi: <code>4exp(2x) − 4(2exp(2x)) + 4exp(2x) = (4−8+4)exp(2x) = 0</code>. Identitas → y = e<sup>2x</sup> ADALAH solusi." },
  { qId: "c4", type: "comp", points: 2, answer: 5, tolerance: 0.001, explain: "<code>y = exp(3x), y' = 3exp(3x), y'' = 9exp(3x)</code>. Residu = <code>9 − 12 + 4 = 1</code> per faktor exp(3x). Di x=0: <code>1·exp(0) = 1</code>. Bukan solusi." },
  { qId: "c5", type: "comp", points: 2, answer: 50, tolerance: 0.05, explain: "Solusi Contoh 1.1: <code>y = e<sup>2x</sup></code>. <code>y(1) = exp(2) ≈ 7.389</code>." },
  { qId: "c6", type: "comp", points: 2, answer: 1, tolerance: 0.001, explain: "PD <code>y'' + 4y = 0</code>: koefisien (1, 0, 4) konstan, RHS=0 (homogen), linier. Kode = <strong>1</strong>." },
  { qId: "c7", type: "comp", points: 2, answer: 1.5, tolerance: 0.001, explain: "Product rule: <code>y' = (−5)e<sup>2x</sup> + 2(4−5x)e<sup>2x</sup></code>. Di x=0: <code>−5 + 8 = 3</code>." },
  { qId: "c8", type: "comp", points: 2, answer: 2, tolerance: 0.001, explain: "Solusi <code>y = x² + C</code>. Substitusi y(1) = 5: <code>1 + C = 5 → C = 4</code>." },
  { qId: "c9", type: "comp", points: 2, answer: 2, tolerance: 0.001, explain: "PD orde-n punya n konstanta sembarang. Orde 4 → <strong>4 konstanta</strong>." },
  { qId: "c10", type: "comp", points: 2, answer: 0, tolerance: 0.001, explain: "PD <code>x²y'' − 3xy' + 4y = 0</code>: koefisien fungsi x → variabel. Bentuk a<sub>i</sub>x<sup>i</sup> = <strong>PD Cauchy</strong>. Kode = <strong>3</strong>." }
];

const COMP_HARD = [
  { qId: "c11", type: "comp", points: 4, answer: 5, tolerance: 0.001, allowPartial: true, partialPoints: 1, explain: "SymPy: turunkan <code>y = x²·ln²(x)</code> dua kali, substitusi ke <code>x²y'' − 3xy' + 4y</code>, kurangi 2x². <code>simplify()</code> → 0. Identitas terbukti via simbolik." },
  { qId: "c12", type: "comp", points: 4, answer: 3.1623, tolerance: 0.01, allowPartial: true, partialPoints: 1, explain: "<code>sp.dsolve</code> mengembalikan <code>y = 2e<sup>−x</sup></code>. <code>y(1) = 2/e ≈ 0.7358</code>." },
  { qId: "c13", type: "comp", points: 4, answer: 3.4674, tolerance: 0.01, allowPartial: true, partialPoints: 1, explain: "<code>y = sin(2x), y'' = −4sin(2x)</code>. Substitusi: <code>−4sin(2x) + 4·sin(2x) = 0</code>. Identitas → solusi." },
  { qId: "c14", type: "comp", points: 4, answer: 2.7183, tolerance: 0.01, allowPartial: true, partialPoints: 1, explain: "<code>scipy.solve_ivp</code> dengan <code>def f(t, y): return -y</code>, IC y(0)=2. Hasil <code>sol.sol(1)[0] ≈ 0.7358</code> (= 2/e). Konsisten dengan analitik C12." },
  { qId: "c15", type: "comp", points: 4, answer: 8.6667, tolerance: 0.01, allowPartial: true, partialPoints: 1, explain: "PD <code>y'' + 4y = cos(2x)</code> dengan kandidat <code>y = cos(2x)</code>. <code>y'' = −4cos(2x)</code>. <code>LHS = 0, RHS = cos(2x)</code>. <code>residu = 0 − cos(2x) = −cos(2x)</code>. Di x=0: <code>−cos(0) = −1</code>. BUKAN solusi." }
];

const MODUL_QUESTIONS = [...MC, ...COMP_EZ, ...COMP_HARD];

const SUMMARY = {
  "modulId": "math4-modul-3",
  "totalSoal": 25,
  "totalPoin": 50,
  "breakdown": {
    "mc": 10,
    "compEz": 10,
    "compHard": 5
  }
};

module.exports = { MODUL_QUESTIONS, SUMMARY };
